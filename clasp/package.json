{
  "name": "money-sheet-gas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/strip-exports.js",
    "push": "npm run build && npx @google/clasp push -f",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.4.5",
    "vitest": "^4.1.7"
  }
}
